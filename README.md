# Agent Backplane

Curated implementation through events retention and recovery. Later deployment and publication material follows in the PR train.

Run `bun install --frozen-lockfile`, `bun run check`, and `bun run test`.
